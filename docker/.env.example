# Credentials used by docker-compose.yml. Keep these in sync with backend/.env.
POSTGRES_USER=reachinbox
POSTGRES_PASSWORD=reachinbox
POSTGRES_DB=reachinbox

# Optional locally. Required for any environment where Redis is not confined
# to the loopback interface. Must match REDIS_URL in backend/.env.
REDIS_PASSWORD=
